<Window x:Class="ZapretWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SupaModd Launcher" Height="580" Width="900"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanMinimize">

    <Window.Resources>
        <!-- Полупрозрачные цвета для эффекта стекла -->
        <SolidColorBrush x:Key="GlassBg" Color="#9918181b"/>
        <SolidColorBrush x:Key="GlassBorder" Color="#33ffffff"/>

        <!-- Акцентные цвета -->
        <SolidColorBrush x:Key="AccentColor" Color="#8b5cf6"/>
        <SolidColorBrush x:Key="AccentColorHover" Color="#a78bfa"/>

        <!-- ================= СТИЛИ КНОПОК ПАНЕЛИ ЗАГОЛОВКА ================= -->
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#a1a1aa"/>
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33ffffff"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBarCloseButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#ef4444"/>
                                <!-- Красный при наведении -->
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ================= СТИЛИ ОБЫЧНЫХ КНОПОК И ПРОЧЕГО (БЕЗ ИЗМЕНЕНИЙ) ================= -->
        <ControlTemplate x:Key="GlassButtonTemplate" TargetType="Button">
            <Border x:Name="border" Background="#26ffffff" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="8" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Background" Value="#33ffffff"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="border" Property="Background" Value="#1affffff"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="GlassButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template" Value="{StaticResource GlassButtonTemplate}"/>
        </Style>

        <ControlTemplate x:Key="AccentButtonTemplate" TargetType="Button">
            <Border x:Name="border" Background="{StaticResource AccentColor}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Background" Value="{StaticResource AccentColorHover}"/>
                    <Setter TargetName="border" Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#8b5cf6" BlurRadius="15" ShadowDepth="0" Opacity="0.5"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="border" Property="Background" Value="#7c3aed"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource GlassButton}">
            <Setter Property="Template" Value="{StaticResource AccentButtonTemplate}"/>
        </Style>

        <ControlTemplate x:Key="DangerButtonTemplate" TargetType="Button">
            <Border x:Name="border" Background="#ef4444" CornerRadius="8" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="border" Property="Background" Value="#f87171"/>
                    <Setter TargetName="border" Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#ef4444" BlurRadius="15" ShadowDepth="0" Opacity="0.5"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="border" Property="Background" Value="#dc2626"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource GlassButton}">
            <Setter Property="Template" Value="{StaticResource DangerButtonTemplate}"/>
        </Style>

        <!-- Toggle Switch -->
        <ControlTemplate x:Key="ToggleSwitchTemplate" TargetType="CheckBox">
            <StackPanel Orientation="Horizontal" Background="Transparent">
                <Border x:Name="SwitchBg" Width="44" Height="24" CornerRadius="12" Background="#40ffffff" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" VerticalAlignment="Center">
                    <Ellipse x:Name="SwitchThumb" Width="16" Height="16" Fill="White" HorizontalAlignment="Left" Margin="3,0,0,0">
                        <Ellipse.RenderTransform>
                            <TranslateTransform x:Name="ThumbTranslate" X="0"/>
                        </Ellipse.RenderTransform>
                    </Ellipse>
                </Border>
                <ContentPresenter Margin="12,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="SwitchBg" Property="Background" Value="{StaticResource AccentColor}"/>
                    <Setter TargetName="SwitchBg" Property="BorderThickness" Value="0"/>
                </Trigger>
                <EventTrigger RoutedEvent="CheckBox.Checked">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="ThumbTranslate" Storyboard.TargetProperty="X" To="20" Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="CheckBox.Unchecked">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="ThumbTranslate" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template" Value="{StaticResource ToggleSwitchTemplate}"/>
        </Style>

        <!-- Вкладки -->
        <ControlTemplate x:Key="TabItemTemplate" TargetType="TabItem">
            <Border Name="Border" Padding="25,12" Margin="0,0,10,0" Background="Transparent" CornerRadius="8">
                <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="#33ffffff"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="#1affffff"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#a1a1aa"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template" Value="{StaticResource TabItemTemplate}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Темный скроллбар -->
        <SolidColorBrush x:Key="ScrollBarBackground" Color="Transparent" />
        <SolidColorBrush x:Key="ScrollBarThumb" Color="#4d4d54" />
        <SolidColorBrush x:Key="ScrollBarThumbHover" Color="#66666d" />
        <SolidColorBrush x:Key="ScrollBarThumbPressed" Color="#8b5cf6" />
        <ControlTemplate x:Key="ScrollBarThumbTemplate" TargetType="{x:Type Thumb}">
            <Border Background="{TemplateBinding Background}" CornerRadius="4" Margin="2" />
        </ControlTemplate>
        <Style x:Key="DarkScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="{StaticResource ScrollBarBackground}"/>
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Width" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Background="{TemplateBinding Background}">
                            <RepeatButton Command="ScrollBar.LineUpCommand" Visibility="Collapsed" />
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Background="{StaticResource ScrollBarThumb}" Template="{StaticResource ScrollBarThumbTemplate}">
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource ScrollBarThumbHover}" />
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource ScrollBarThumbPressed}" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                            <RepeatButton Command="ScrollBar.LineDownCommand" Visibility="Collapsed" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanContentScroll="{TemplateBinding CanContentScroll}"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" HorizontalAlignment="Right" VerticalAlignment="Stretch" Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Style="{StaticResource DarkScrollBar}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="30" />
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" Grid.ColumnSpan="2" CornerRadius="8" Background="#26ffffff" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" />
                <Border Grid.Column="0" CornerRadius="8,0,0,8" Margin="1" Background="Transparent" />
                <Path x:Name="Arrow" Grid.Column="1" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M0,0 L0,2 L4,6 L8,2 L8,0 L4,4 z" />
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="ToggleButton.IsMouseOver" Value="true">
                    <Setter TargetName="Border" Property="Background" Value="#33ffffff" />
                </Trigger>
                <Trigger Property="ToggleButton.IsChecked" Value="true">
                    <Setter TargetName="Border" Property="Background" Value="#40ffffff" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="GlassComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" Template="{StaticResource ComboBoxToggleButton}" Grid.Column="2" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press"/>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="15,0,30,0" VerticalAlignment="Center" HorizontalAlignment="Left" />
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#1f1f22" BorderThickness="1" BorderBrush="{StaticResource GlassBorder}" CornerRadius="8" Margin="0,5,0,0"/>
                                    <ScrollViewer Margin="4,9,4,4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Name="Border" Padding="10,8" Margin="0,2" SnapsToDevicePixels="true" CornerRadius="5" Background="Transparent">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#3f3f46" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentColor}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Обертка всего окна для закругленных краев (ВМЕСТО СТАНДАРТНЫХ УГЛОВ WINDOWS) -->
    <Border CornerRadius="12" Background="#09090b" BorderBrush="#33ffffff" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <!-- Фоновые свечения -->
            <Rectangle Margin="-200,-200,0,0" Width="600" Height="600" HorizontalAlignment="Left" VerticalAlignment="Top" Opacity="0.15">
                <Rectangle.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#8b5cf6" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </RadialGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <Rectangle Margin="0,0,-200,-200" Width="500" Height="500" HorizontalAlignment="Right" VerticalAlignment="Bottom" Opacity="0.1">
                <Rectangle.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#3b82f6" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </RadialGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <Grid>
                <!-- КАСТОМНАЯ ШАПКА ОКНА (С перетаскиванием мыши) -->
                <!-- Верхняя шапка -->
                <Border Height="70" VerticalAlignment="Top" Background="#80000000" BorderBrush="{StaticResource GlassBorder}" BorderThickness="0,0,0,1" 
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                    <Grid>
                        <!-- Логотип (Картинка) -->
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="25,0,0,0" IsHitTestVisible="False">
                            <!-- Наша картинка логотипа -->

                            <!-- Плашка с версией (оставим её для стиля, она смотрится круто рядом с лого) -->
                            <Image Source="/logo.png" Height="40" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality" Width="679"/>

                            <!-- Плашка с версией (оставим её для стиля, она смотрится круто рядом с лого) -->
                            <Border Background="#26ffffff" CornerRadius="6" Padding="6,2" Margin="15,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="v1.0" FontSize="13" FontWeight="Bold" Foreground="{StaticResource AccentColor}" RenderTransformOrigin="-1.704,0.789"/>
                            </Border>
                        </StackPanel>

                        <!-- Кнопки управления окном -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0">
                            <!-- Свернуть -->
                            <Button Content="—" Style="{StaticResource TitleBarButton}" Margin="0,0,5,0" Click="BtnMinimize_Click"/>
                            <!-- Закрыть -->
                            <Button Content="✕" Style="{StaticResource TitleBarCloseButton}" Click="BtnClose_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Основной TabControl -->
                <TabControl x:Name="MainTabControl" Background="Transparent" BorderThickness="0" Margin="20,90,20,20">

                    <!-- ВКЛАДКА 1: ОБХОД DPI -->
                    <TabItem>
                        <TabItem.Header>
                            <TextBlock Text="Обход DPI" FontSize="16" FontWeight="SemiBold" Padding="10,2"/>
                        </TabItem.Header>
                        <Grid Margin="0,20,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="12" Padding="25">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center" Margin="0,0,40,0">
                                        <CheckBox x:Name="chkDiscord" Content="Разблокировать Discord" IsChecked="True" Style="{StaticResource ToggleSwitch}" Margin="0,0,0,15"/>
                                        <CheckBox x:Name="chkYouTube" Content="Разблокировать YouTube" IsChecked="True" Style="{StaticResource ToggleSwitch}" Margin="0,0,0,15"/>
                                        <CheckBox x:Name="chkTelegram" Content="Разблокировать Telegram" IsChecked="False" Style="{StaticResource ToggleSwitch}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Метод обхода (Стратегия):" Foreground="#a1a1aa" FontSize="14" Margin="0,0,0,8"/>
                                        <ComboBox x:Name="cmbStrategy" Height="40" FontSize="15" SelectedIndex="5" Style="{StaticResource GlassComboBox}">
                                            <ComboBoxItem Content="General"/>
                                            <ComboBoxItem Content="Alternative 1"/>
                                            <ComboBoxItem Content="Alternative 2"/>
                                            <ComboBoxItem Content="Alternative 3"/>
                                            <ComboBoxItem Content="Alternative 4 (на тех. обслуживании)"/>
                                            <ComboBoxItem Content="Supamodd 1"/>
                                        </ComboBox>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5,15,0,5">
                                <TextBlock Text="Логи системы:" Foreground="#a1a1aa" FontSize="14" VerticalAlignment="Center"/>
                                <Button Content="🌐 Пинг" Style="{StaticResource GlassButton}" Padding="15,4" Margin="15,0,0,0" FontSize="12" Click="BtnTestConnection_Click"/>
                                <Button Content="🔄 Обновить списки" Style="{StaticResource GlassButton}" Padding="15,4" Margin="10,0,0,0" FontSize="12" Click="BtnUpdateLists_Click"/>
                            </StackPanel>

                            <Border Grid.Row="2" Background="#80000000" CornerRadius="10" BorderThickness="1" BorderBrush="#26ffffff">
                                <TextBox x:Name="txtLogs" Background="Transparent" Foreground="#a78bfa" 
                                         IsReadOnly="True" TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Auto" 
                                         FontFamily="Consolas" BorderThickness="0" Padding="15" FontSize="14"/>
                            </Border>

                            <Grid Grid.Row="3" Margin="0,15,0,0">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button Content="▶ Тест в окне" Width="140" Style="{StaticResource AccentButton}" Margin="0,0,15,0" Click="BtnStart_Click"/>
                                    <Button Content="⏹ Остановка" Width="120" Style="{StaticResource GlassButton}" Click="BtnStop_Click"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="🛡 Установить Службу" Width="190" Style="{StaticResource GlassButton}" Margin="0,0,15,0" Click="BtnInstallService_Click"/>
                                    <Button Content="Удалить Службу" Width="150" Style="{StaticResource DangerButton}" Click="BtnRemoveService_Click"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </TabItem>

                    <!-- ВКЛАДКА 2: НАСТРОЙКИ DNS -->
                    <TabItem>
                        <TabItem.Header>
                            <TextBlock Text="Настройки DNS" FontSize="16" FontWeight="SemiBold" Padding="10,2"/>
                        </TabItem.Header>
                        <Grid Margin="0,20,0,0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,60,0,0" Height="279" Width="693">

                                <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="15" Width="275" Height="269" Margin="0,0,25,0" Padding="25">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="🧹" FontSize="45" HorizontalAlignment="Center" Margin="0,0,0,15">
                                            <TextBlock.Effect>
                                                <DropShadowEffect Color="White" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                                            </TextBlock.Effect>
                                        </TextBlock>
                                        <TextBlock Text="Сброс сети" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                        <TextBlock Text="Очистка кэша DNS для решения проблем с загрузкой страниц." TextWrapping="Wrap" TextAlignment="Center" Foreground="#a1a1aa" FontSize="13" Margin="0,0,0,20"/>
                                        <Button Content="Очистить" Style="{StaticResource GlassButton}" Padding="10,8" Click="BtnFlushDns_Click"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="15" Width="301" Height="259" Padding="20">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="🔒" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10">
                                            <TextBlock.Effect>
                                                <DropShadowEffect Color="White" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                                            </TextBlock.Effect>
                                        </TextBlock>
                                        <TextBlock Text="Сменить DNS" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                                        <TextBlock Text="Установите безопасные DNS-серверы для обхода базовых блокировок." TextWrapping="Wrap" TextAlignment="Center" Foreground="#a1a1aa" FontSize="12" Margin="0,0,0,10"/>

                                        <ComboBox x:Name="cmbDnsSelection" Height="32" FontSize="13" SelectedIndex="0" Style="{StaticResource GlassComboBox}" Margin="0,0,0,10">
                                            <ComboBoxItem Content="Cloudflare (1.1.1.1)"/>
                                            <ComboBoxItem Content="Google DNS (8.8.8.8)"/>
                                            <ComboBoxItem Content="XBOX DNS (111.88.96.50)"/>
                                            <ComboBoxItem Content="По умолчанию (Авто)"/>
                                        </ComboBox>

                                        <Button Content="Применить DNS" Style="{StaticResource AccentButton}" Padding="10,6" Click="BtnSetDns_Click"/>
                                    </StackPanel>
                                </Border>

                            </StackPanel>
                        </Grid>
                    </TabItem>

                    <!-- ВКЛАДКА 3: ДРУГИЕ ОБХОДЫ -->
                    <TabItem>
                        <TabItem.Header>
                            <TextBlock Text="Другое" FontSize="16" FontWeight="SemiBold" Padding="10,2"/>
                        </TabItem.Header>
                        <Grid Margin="0,20,0,0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,60,0,0" Height="265" Width="657">

                                <!-- Карточка: Instagram -->
                                <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="15" Width="303" Height="257" Margin="0,0,25,0" Padding="25">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="📸" FontSize="45" HorizontalAlignment="Center" Margin="0,0,0,15">
                                            <TextBlock.Effect>
                                                <DropShadowEffect Color="White" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                                            </TextBlock.Effect>
                                        </TextBlock>
                                        <TextBlock Text="Обход Instagram" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                        <TextBlock Text="Добавляет актуальные IP-адреса CDN Инстаграма в системный файл hosts." TextWrapping="Wrap" TextAlignment="Center" Foreground="#a1a1aa" FontSize="12" Margin="0,0,0,20"/>
                                        <Button Content="Установить патч" Style="{StaticResource AccentButton}" Padding="10,8" Click="BtnPatchInstagram_Click"/>
                                    </StackPanel>
                                </Border>

                                <!-- Карточка: Разное (Секретная кнопка) -->
                                <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="15" Width="301" Height="258" Padding="25">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="🎭" FontSize="45" HorizontalAlignment="Center" Margin="0,0,0,15">
                                            <TextBlock.Effect>
                                                <DropShadowEffect Color="White" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                                            </TextBlock.Effect>
                                        </TextBlock>
                                        <TextBlock Text="Разблокировка Медиа" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                        <TextBlock Text="Добавляет популярные заблокированные ресурсы и галереи в фильтр обхода DPI." TextWrapping="Wrap" TextAlignment="Center" Foreground="#a1a1aa" FontSize="12" Margin="0,0,0,20"/>
                                        <Button Content="Активировать" Style="{StaticResource GlassButton}" Padding="10,8" Click="BtnMediaBypass_Click"/>
                                    </StackPanel>
                                </Border>

                            </StackPanel>
                        </Grid>
                    </TabItem>

                </TabControl>
            </Grid>
        </Grid>
    </Border>
</Window>